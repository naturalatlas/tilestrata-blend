language: node_js
sudo: required
node_js:
  - "0.11"
  - "0.10"
before_install:
  - sudo apt-get install libmapnik2.2
install:
  - npm install tilestrata
  - npm install
script:
  - make test && (make test-ci-coverage || true)
